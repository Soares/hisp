(%if ie 6 ~ (abc))
(a (:href http://google.com) "test")
(%goto object ~ {object})
(%internal (:url test)
           (abc)
           (def))
(%goto object ~ {object})
(%link .label ~ App.Label)
{    %    test}
{#django comment}
(!html comment)
(@let's see you parse this!)
{!fuckin gone}
(%internal "abc "{one}" two")
