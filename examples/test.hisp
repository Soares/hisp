(%if[ie 6] (abc))

(a (:href http://google.com) "test")
(%goto[object] {object})
(%link[.label] App.Label)

(%internal (:action test) (abc) (def))
(%internal[%test] (abc) (def))
(%internal[test] (abc) (def))

"I have a string. {there"s a var} with double quotes. Problem?"
